import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.blogProject.blogApp',
  appName: 'B-log',
  webDir: 'www',
  server: {
    androidScheme: 'https',
  },
};

export default config;
